import { HttpLoginterceptor } from './http-loginterceptor';

describe('HttpLoginterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpLoginterceptor()).toBeTruthy();
  });
});
